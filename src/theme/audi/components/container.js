import { ComponentStyleConfig } from "@chakra-ui/react"
const Container= {
    baseStyle: {
        wrapper: {
            px: { base: 5, xl: 4 },
            maxW: '7xl',
            mx: 'auto',
            w: '100%',
        }   
    }
}

export default Container